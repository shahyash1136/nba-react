import React from 'react';
import './Footer.scss';

const footer = (props) => {
    return (
        <footer>
            <div className="footer__top">
            </div>
            <div className="footer__bottom">
            </div>
        </footer>
    );
}

export default footer;